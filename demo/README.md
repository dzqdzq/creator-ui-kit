# UI-Kit Demo 使用说明

## 🚀 启动方式

由于 Demo 使用了 ES Modules，需要通过 HTTP 服务器运行，不能直接用 `file://` 协议打开。

### 方法 1: 使用项目自带的服务器（推荐）

```bash
# 在项目根目录运行
pnpm run demo
```

或者：

```bash
node demo/server.js
```

然后访问: http://localhost:3000/demo/index.html

### 方法 2: 使用 Python HTTP 服务器

如果你安装了 Python 3:

```bash
# 在项目根目录运行
pnpm run demo:python
```

或者：

```bash
cd demo
python3 -m http.server 3000
```

然后访问: http://localhost:3000/index.html

### 方法 3: 使用 Python 2（如果只有 Python 2）

```bash
pnpm run demo:python2
```

或者：

```bash
cd demo
python -m SimpleHTTPServer 3000
```

### 方法 4: 使用 Node.js http-server（需要全局安装）

```bash
# 安装 http-server
npm install -g http-server

# 在项目根目录运行
http-server -p 3000
```

然后访问: http://localhost:3000/demo/index.html

### 方法 5: 使用 Vite 开发服务器

```bash
# 在项目根目录运行
pnpm run preview
```

注意：需要先运行 `pnpm run build` 构建项目。

## 📁 文件结构

```
demo/
├── index.html              # 导航首页
├── basic-forms.html        # 基础表单元素
├── numeric-inputs.html     # 数值输入与滑块
├── color-picker-demo.html  # 颜色选择器
├── layout-containers.html  # 布局与容器
├── progress-loader.html    # 进度与加载
├── property-panel.html     # 属性面板
├── other-elements.html    # 其他元素
├── server.js              # 简单的 HTTP 服务器
└── README.md              # 本文件
```

## ⚠️ 注意事项

1. **必须使用 HTTP 服务器**：ES Modules 不支持 `file://` 协议，必须通过 HTTP/HTTPS 访问
2. **路径问题**：所有 demo 文件都使用相对路径 `../dist/ui-kit.js` 引用构建后的文件
3. **构建文件**：确保已经运行 `pnpm run build` 生成了 `dist/ui-kit.js`

## 🔧 故障排除

### 问题：无法加载 ui-kit.js

**解决方案：**
1. 确保已经运行 `pnpm run build` 构建项目
2. 检查 `dist/ui-kit.js` 文件是否存在
3. 确保使用 HTTP 服务器而不是直接打开文件

### 问题：CORS 错误

**解决方案：**
- 必须使用 HTTP 服务器，不能直接用 `file://` 协议打开
- 使用上述任一方法启动服务器

### 问题：端口被占用

**解决方案：**
- 修改 `server.js` 中的 `PORT` 变量
- 或使用其他端口：`python3 -m http.server 8080`

## 📝 开发建议

- 修改 demo 文件后，刷新浏览器即可看到变化
- 修改源代码后，需要重新运行 `pnpm run build` 并刷新浏览器

